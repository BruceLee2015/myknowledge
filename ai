基础算子：如加法（Add）、减法（Subtract）、乘法（Multiply）、除法（Divide）等。

矩阵和向量算子：如矩阵乘法（MatMul）、向量加权求和（Dot Product）、转置（Transpose）等。

卷积算子：如二维卷积（Conv2D）、三维卷积（Conv3D）等，这些算子在处理图像和视频数据时非常重要。

池化算子：如最大池化（MaxPooling）、平均池化（AveragePooling）等，用于降低特征维度并提取重要特征。

激活函数：如ReLU（Rectified Linear Unit）、Sigmoid、Tanh等，用于引入非线性，使得神经网络能够学习复杂的模式。

归一化算子：如批量归一化（Batch Normalization）、层归一化（Layer Normalization）等，用于调整神经网络中间层的输出，以提高训练的稳定性和速度。

损失函数算子：如均方误差（MSE）、交叉熵（Cross-Entropy）等，用于评估模型的预测与真实值之间的差异。

优化算子：如梯度下降（Gradient Descent）、Adam等，用于更新网络权重以最小化损失函数。
